A. Retrieval + physics constraints 1. Given Sentinel-1 C-band limits over vegetation, what “best practice” signal decomposition would you recommend: simple VV-only, VV/VH ratio, RVI, or a canopy correction approach? 2. What’s your take on using a water cloud model (WCM) style correction (even approximate) before ML, vs letting ML learn it implicitly? 3. Incidence angle + geometry: should I explicitly include local incidence angle / orbit direction / look angle features, or is DEM slope/aspect enough? 4. Surface roughness confounding: are my “roughness proxies” (rolling abs change of VV/VH, spike timing) reasonable, or is there a better roughness feature family? 5. Freeze/thaw and snow: what screening or flags would you apply (snow cover, temperature, SWE) if I’m trying to stay “no ground sensors”? 6. Temporal lag reality: for your experience, what lag horizons between rain and SAR response are actually meaningful at 5 cm?

B. SMAP fusion (your new add) 7. When fusing SMAP with S1, do you prefer using SMAP as a prior (baseline + residual learning), or as just another feature? 8. AM vs PM: is there a known bias or stability difference you’d recommend (AM only, PM only, or combined)? 9. Gap filling: is my approach (imputed SMAP + mask features) the right move, or should I avoid imputation and only use “available” points? 10. Spatial mismatch: SMAP footprint vs station point. Any recommended downscaling strategy that’s lightweight (no deep learning), or should I treat SMAP as a coarse context feature?

C. Modeling strategy and evaluation 11. If you had my setup (temporal split, few stations), what model family would you bet on: gradient boosting, linear+interactions, state-space, or something else? 12. Station generalization: should I leave one station out as a stress test, even if it hurts score, to see if the model is actually learning transferable structure? 13. Target transform: would you recommend predicting anomalies (remove seasonal climatology per station) then adding back, vs raw moisture? 14. Feature leakage audit: can you sanity-check my “train-only” seasonal anomaly/zscore design? Any subtle leak patterns you’ve seen in similar work? 15. Feature importance sanity: what should “rise to the top” if the model is behaving physically? (VV? API? SMAP? NDMI?).

D. Concrete next experiments to validate 16. Can you suggest 2–3 ablations that would be most diagnostic (ex: remove SMAP entirely, VV only, remove optical, etc.)? 17. If score improves with SMAP, how do I prove it’s not just “SMAP is already close to truth” and I’m learning a trivial mapping? 18. Any recommended metrics beyond R² for this (bias, seasonal phase error, event response), especially for soil moisture?
