# July 14, 2026

## Hyperparameter Sweep
### Fewer Iteration
Tried the SOTA combination from `derived_8.2-hyperparameters-1.3` with different number of steps, turns out 1500 steps is enough to achieve similar performance to 8000 steps, saving about 80% of computational cost.

So even with lower learning rate, we don't really need that many steps.

## Alternative Gating
Since the soil moisture-based gating is essentially the single global model, so to do it meaningfully we must use something else.

## Soil moisture-based regime
Just realized a new issue when I run the feature selection. That the feature set is based on the true-labeled, so if the gating done imperfectly then the routed data probably not aligned with the selected features.  

Also the feature selection is rather a failure especially for 3-regime, see `/notebooks/experiment/derived_8.2-feature-selection/selected-regime-features-v1.md` for more details.