# July 27, 2026

## Results from a finally good feature-selection experiment
- Yes, a shared global backbone currently yield the best performance. 
- Select features solely within each cluster collapses because less training data → more aggressive regularization → fewer survivors (i.e. far less features than the shared backbone) → worse performance.
- Even then per-cluster add-only deltas only helps marginally for the smaller cluster, and no help for the larger cluster.