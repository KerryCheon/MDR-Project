# June 26, 2026

## Continue on dataset: derived_8.1
### Feature Selection:
- Ran for all 3 regimes (wet, dry, normal) and all stations in derived_8.1 dataset.
### `experiment/derived_8.1-eval-1/`
- Single global model + 3 regime-specific models (wet, dry, normal) trained on derived_8.1 dataset.
- Achieved R2 of 0.852 on the oracle hard gating 3-regime model, compared to 0.415 on the single global model. 
- The *"Residuals vs True SM | Hard Gated (Oracle)"* plot shows that the 3-regime model is working as the residuals are pooled to zero so it's useful to set some kind of boundaries for different soil moisture values. However, there's high concentration of residuals at SM=0.0, so its worth to see what happens if we remove those points and re-run the experiment.
- No other pre-processing was done on the dataset, so there're still a lot of space for improvement. For example, might add the temporal drifting similar to v24 and v25.

## Proposal for new dataset: derived_8.1_pos
- Treat soil moisture value of 0.0 are Nan/invalid, essentially removing those points from the dataset. 