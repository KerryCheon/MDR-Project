# August 5, 2026

## MLP
Doing hyperparameters sleeps with MLP models for both one regime and two regimes.

Performance around 0.7 R^2, still a long way to go.

One carry out is that it requires some temporal holdout from the testval set for early stopping. But even then, the model with good validation performance are simply overfeed on 10% validation set -> still poor generalization performance.