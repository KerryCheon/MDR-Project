# July 2, 2026

## New Gating Proxy
- Need to make a end-to-end model.
- The other proxy don't really mimick the <u>soil moisture</u> separation:
    - Seasons alone are insufficient: 49%
    - Even Random Forest (Month + G_API + LST + SMAP) only achieves 58% accuracy.
    - These features alone don't create much separation between the three classes.
    - Transition class is poorly separable, so binary MoE might win again.